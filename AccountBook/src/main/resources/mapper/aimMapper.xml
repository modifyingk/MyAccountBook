<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aimMapper">
	<!-- 목표 카테고리 중복 확인 -->
	<select id="overlapAim" parameterType="aimVO" resultType="String">
		select bigcate from aim where bigcate = #{bigcate} and userid = #{userid};
	</select>

	<!-- 목표 추가 -->
	<insert id="insertAim" parameterType="aimVO">
		insert into aim values (#{aimid}, #{bigcate}, #{total}, #{userid});
	</insert>
	
	<!-- 목표 수정 -->
	<update id="updateAim" parameterType="aimVO">
		update aim set total = #{total} where bigcate = #{bigcate} and userid = #{userid};
	</update>
	
	<!-- 목표 삭제 -->
	<delete id="deleteAim" parameterType="aimVO">
		delete from aim where bigcate = #{bigcate} and userid = #{userid};
	</delete>

	<!-- 목표 가져오기 -->
	<select id="selectAim" parameterType="aimVO" resultType="aimjoinVO">
		select aimid, bigcate, total as aim, userid,
		ifnull((select (sum(total) * -1) from account where account.userid = aim.userid and account.bigcate = aim.bigcate and account.moneytype = "지출" and left(account.date, 6) = #{date}), 0) as spend
		from aim where userid = #{userid};
	</select>
</mapper>