<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repeatMapper">
	<!-- 반복 중복 확인 -->
	<select id="overlapRepeat" parameterType="repeatVO" resultType="repeatVO">
		select repeatid from repetition where moneytype = #{moneytype} and date = #{date} and assetname = #{assetname}
		and bigcate = #{bigcate} and smallcate = #{smallcate} and content = #{content} and total = #{total} and userid = #{userid};
	</select>

	<!-- 반복 추가 -->
	<insert id="insertRepeat" parameterType="repeatVO">
		insert into repetition values (#{repeatid}, #{repeatcycle}, #{moneytype}, #{date}, #{assetname}, #{bigcate}, #{smallcate}, #{content}, #{total}, #{userid});
	</insert>
	
	<!-- 반복 내역 가져오기 -->
	<select id="selectRepeat" parameterType="repeatVO" resultType="repeatVO">
		select * from repetition where userid = #{userid} and (repeatcycle = "매월" or substring(date, 1, 2) = #{date} and repeatcycle = "매년") order by date;
	</select>
	
	<!--반복 삭제 -->
	<delete id="deleteRepeat" parameterType="repeatVO">
		delete from repetition where repeatid = #{repeatid} and userid = #{userid};
	</delete>
	
	<!-- 오늘의 반복 내역 가져오기 -->
	<select id="todayRepeat" parameterType="repeatVO" resultType="accountVO">
		select moneytype, date, assetname, bigcate, smallcate, content, total, userid from repetition
		where repeatcycle = #{repeatcycle} and date = #{date};
	</select>
</mapper>